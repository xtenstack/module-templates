{
    "name": "xtenstack/application-template",
    "description": "Blank/template application-tier module demonstrating the app_skeleton module contract (see MODULE-SPEC.md) — copy this as a starting point for a real module, not a feature to depend on directly.",
    "type": "library",
    "license": "MIT",
    "minimum-stability": "dev",
    "require": {
        "php": "^8.3"
    },
    "autoload": {
        "psr-4": {
            "XtenApplicationTemplate\\": "src/"
        }
    }
}
