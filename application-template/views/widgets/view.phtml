<div class="row mb-2">
  <div class="col-12 d-flex justify-content-between align-items-center">
    <h1><?= $this->escaper->escapeHtml($widget->name) ?></h1>
    <div>
      <a href="<?= $this->url->get('widgets/widgets/edit/' . $widget->id) ?>" class="btn btn-outline-secondary">Edit</a>
      <a href="<?= $this->url->get('widgets/widgets') ?>" class="btn btn-link">Back to list</a>
    </div>
  </div>
</div>

<div class="row">
  <div class="col-12 col-md-8">
    <div class="card">
      <div class="card-body">
        <dl class="row mb-0">
          <dt class="col-sm-3">Description</dt>
          <dd class="col-sm-9"><?= $widget->description ? nl2br($this->escaper->escapeHtml($widget->description)) : '<span class="text-muted">—</span>' ?></dd>

          <dt class="col-sm-3">Created</dt>
          <dd class="col-sm-9"><?= $this->escaper->escapeHtml($widget->created_at) ?></dd>

          <dt class="col-sm-3">Updated</dt>
          <dd class="col-sm-9"><?= $this->escaper->escapeHtml($widget->updated_at) ?></dd>
        </dl>
      </div>
    </div>

    <form method="post" action="<?= $this->url->get('widgets/widgets/delete/' . $widget->id) ?>" onsubmit="return confirm('Delete <?= $this->escaper->escapeHtmlAttr($widget->name) ?>?')">
      <button type="submit" class="btn btn-outline-danger mt-2">Delete</button>
    </form>
  </div>
</div>
