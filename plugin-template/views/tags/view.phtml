<div class="row mb-2">
  <div class="col-12 d-flex justify-content-between align-items-center">
    <h1><?= $this->escaper->escapeHtml($tag->name) ?></h1>
    <div>
      <a href="<?= $this->url->get('tags/tags/edit/' . $tag->id) ?>" class="btn btn-outline-secondary">Edit</a>
      <a href="<?= $this->url->get('tags/tags') ?>" class="btn btn-link">Back to list</a>
    </div>
  </div>
</div>

<div class="row">
  <div class="col-12 col-md-8">
    <div class="card">
      <div class="card-body">
        <dl class="row mb-0">
          <dt class="col-sm-3">Description</dt>
          <dd class="col-sm-9"><?= $tag->description ? nl2br($this->escaper->escapeHtml($tag->description)) : '<span class="text-muted">—</span>' ?></dd>

          <dt class="col-sm-3">Created</dt>
          <dd class="col-sm-9"><?= $this->escaper->escapeHtml($tag->created_at) ?></dd>

          <dt class="col-sm-3">Updated</dt>
          <dd class="col-sm-9"><?= $this->escaper->escapeHtml($tag->updated_at) ?></dd>
        </dl>
      </div>
    </div>

    <form method="post" action="<?= $this->url->get('tags/tags/delete/' . $tag->id) ?>" onsubmit="return confirm('Delete <?= $this->escaper->escapeHtmlAttr($tag->name) ?>?')">
      <button type="submit" class="btn btn-outline-danger mt-2">Delete</button>
    </form>
  </div>
</div>
